name: Generate Snake
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake animation
        uses: Platane/snk@v3
        with:
          github_user_name: RED-SHADOW-CYBER-FORCE-IDN
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=ff0000&color_dots[]=0d1117,0d1117,003300,006600,00ff41
            dist/github-contribution-grid-snake-dark.svg?color_snake=ff0000&color_dots[]=0d1117,0d1117,003300,006600,00ff41

      - name: Push to output branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: output
